info:
  name: Get Player
  type: http
  seq: 3

http:
  method: GET
  url: "{{spf_host}}/getplayer/QB-12"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
