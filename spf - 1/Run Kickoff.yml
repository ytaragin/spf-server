info:
  name: Run Kickoff
  type: http
  seq: 9

http:
  method: POST
  url: "{{spf_host}}/game/play"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
