info:
  name: Set Kickoff Def Call
  type: http
  seq: 8

http:
  method: POST
  url: "{{spf_host}}/defense/call"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
