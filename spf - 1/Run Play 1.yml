info:
  name: Run Play 1
  type: http
  seq: 15

http:
  method: POST
  url: "{{spf_host}}/game/play"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: none
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
