info:
  name: Set Off Call - Run
  type: http
  seq: 13

http:
  method: POST
  url: "{{spf_host}}/offense/call"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "play_type": "IR",
        "strategy": "NoStrategy",
        "target": "B1"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
