info:
  name: Get Team Players
  type: http
  seq: 24

http:
  method: GET
  url: "{{spf_host}}/players/home"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
