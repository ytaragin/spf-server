info:
  name: Get State
  type: http
  seq: 2

http:
  method: GET
  url: "{{spf_host}}/game/state"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
