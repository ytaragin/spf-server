info:
  name: Get Off Lineup
  type: http
  seq: 20

http:
  method: GET
  url: "{{spf_host}}/offense/lineup"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
